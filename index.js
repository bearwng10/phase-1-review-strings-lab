// Write your code in this file!
// index.js

// Define the currentUser variable with a string value
const currentUser = 'John Doe';

// Define the welcomeMessage variable using the currentUser variable
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define the excitedWelcomeMessage variable using the currentUser variable
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Define the shortGreeting variable using the currentUser variable
const shortGreeting = `Welcome, ${currentUser[0]}!`;
